# A2.2. Объединение HU и разделение по шву

Дата: 15.09.2026. Статус: DRAFT к реализации, не выполнено. Тип: architecture; родитель прежнего плана: A2. Обязателен [[requirements/nikora_sprint_contract]]. Общий план: [[roadmap/nikora_agent_delivery_plan]].

## 1. Результат и границы

**Законченный проверяемый результат:** Два HU объединены и разобраны по исходным границам, исходный состав и прослеживаемость восстановлены.

Входит:

- Команды aggregate N→1 и seam-split 1→N с атомарной активной иерархией и immutable history.
- Один parent, отсутствие циклов, один магазин, совместимость; верхние HU для capacity.
- Версионированный XML-профиль с исправленной cardinality, не изменение старой схемы на месте.

Не входит: Нет произвольной перепаковки, финальных экранов или изменения регуляторного API.

Зависимости для READY: [A2.1](a2-1.md). Требуются решения бизнес-владельцев и реальные контрагенты/устройства, указанные ниже. BLOCKED/CONTRACT_READY не превращается в ACCEPTED по эмулятору.

## 2. Интерфейс и данные

Предлагаются POST /api/nikora/handling-units/aggregate и /seam-split: commandId, expectedVersions, inputHuIds, output identifiers. Ответ operationId/outputHuIds/newVersions/balance; server rejects cycle/owner/store/loaded/parent conflict.

Все новые URL/имена — предложение к согласованию с действующим API, не описание уже реализованного endpoint. Любая write-команда сохраняет expectedVersion, correlation и аудит. Контракт не ослабляется ради удобства UI.

Точки расширения (перед ticket выбрать точный allowlist, не переписывать папку целиком):

- api/wms_api_server/app/services/ (HU commands)
- db/migrations/
- tests/nikora/

## 3. Ограниченные шаги реализации

1. X1: контракт дерева/операции и balance invariants.
2. H2: реализации aggregate/seam-split + staged snapshots.
3. X3: независимые property/concurrency/Oracle tests.

Каждый шаг — отдельный handoff с patch/test evidence. Local подзадачи только по готовому контракту; изменение инварианта/миграции переводится в H/X. Максимальный scope и stop-rule — в общем контракте.

## 4. Проверки и критерий приёмки

1. 2→1→2, повтор commandId, конкуренция двух родителей, цикл, чужой магазин.
2. Сбой между событием и snapshots не выпускает неполный HU.
3. Top-HU capacity не складывает parent+child; неизменность loaded HU.

Дополнительно обязательны права/дубли/ошибка связи/конкурентная версия, legacy regression и SQL review по общему контракту. Приёмку подтверждают независимый reviewer и владелец сценария; авторский summary недостаточен.

Целевые test artifacts после реализации: tests/nikora/test_a2_2.py и, если UI входит в scope, tests/ui/nikora_a2_2.cjs; плюс Oracle verify SQL и reports/nikora/A2.2/<run>/. Это **планируемые** файлы, их наличие/прохождение сейчас не заявляется.

## 5. Кто программирует и тестирует

Профиль **X — Критические инварианты**.

- Основной исполнитель: Codex gpt-6-astra high: контракт/опасное ядро; gpt-5.6-sol high: реализация.
- Независимая проверка/тесты: Claude Code claude-opus-5 high + Oracle concurrency tests.
- Локальные помощники: только утверждённые механические/изолированные patches и fixtures. Детерминированные pytest/Playwright/SQL runner не расходуют LLM-токены сами по себе.
- Установка и параметры: [[runbooks/nikora_agent_launch]]. Для решения нового класса сначала S0/shadow test, затем повторное использование профиля.

## 6. Время и стоимость — оценка, не обещание

Рабочие дни: **3 / 4 / 7** (нижняя / базовая / верхняя оценка). Базовое окно в последовательном плане: дни **27–30** от готовности ресурсов. Внешнее ожидание не включено. Спринт включает создание тестов, review, исправления и техническую приёмку.

Ориентиры: 21.60 агент-часов (включая tool/test ожидание), 1.80 локальных server-hours, 21.60 часов техлида/QA. Эти часы **не суммируются** в календарную длительность: часть работы идёт параллельно, время людей уже учтено в базовых днях.

| Роль | Модель | Вход, млн | Выход, млн | API $, базовая cache-модель |
|---|---|---:|---:|---:|
| решение, сложное ядро, разбор отказов | gpt-6-astra | 2.700 | 0.270 | 26.93 |
| ограниченные implementation tickets | gpt-5.6-sol | 3.600 | 0.360 | 14.36 |
| независимая проверка инвариантов | claude-opus-5 | 1.800 | 0.180 | 8.98 |
| fixtures без изменения инвариантов | Локальная Qwen | 0.900 | 0.090 | 0.00 |

**API: $50.27**. Локальное электричество: $0.14. **Денежные расходы всего: $50.42; бюджет с резервом 50%: $75.63.** Консервативный вариант без cache hits, с платной записью cache и тем же резервом: $132.52.

Параметры: 60% cache read / 15% cache write / 25% uncached; токены указаны суммарно по всем сессиям этого спринта, не размер одного контекста. Output включает оплачиваемое рассуждение, если провайдер его учитывает. Смета API Standard, без подписок/налогов/инструментальных cloud fees/внешних подрядчиков. Ваше время показано в часах, без перевода в доллары. Для локальной модели учитывается только электричество; покупка и амортизация не учитываются. Цены и формулы: [[roadmap/nikora_agent_delivery_plan]]; исходные данные [budget.json](../../../wiki-raw/nikora_agent_plan_20260915/budget.json).

## 7. Условия остановки и передачи

Остановить ticket при повторяющемся дефекте, выходе за контракт/allowlist, нарушении баланса/прав, отсутствии обязательного внешнего факта или исчерпании бюджета. Не ставить зелёный статус и не менять тест под ошибочный код. Handoff: сделанное, diff, failing case, израсходованные токены/деньги, следующий ограниченный шаг. Новый функционал сверх scope — отдельная карточка с пересчётом.
