# TASK: <sprint>/<ticket>

## Контекст и разрешение

- Исходная ревизия: <SHA>; существующий patch: <hash/location>.
- Карточка спринта: <path>; общий договор: wiki/requirements/nikora_sprint_contract.md.
- Один законченный результат: <наблюдаемое поведение>.
- Разрешённые для правки файлы: <явный список>.
- Только для чтения: <контракты, минимальные примеры>.
- Запрещено: production, реальные секреты/данные, unrelated fixes, новая архитектура вне ADR, commit/push/deploy/install, неоговорённые изменения схемы.
- Модель/оболочка/effort: <точные значения>; максимальный контекст: <tokens>.
- Бюджет одной сессии: <USD/input/output>; остаток бюджета карточки: <USD>.
- Кто и чем обеспечивает денежный лимит: <CLI print cap / gateway / наблюдающий человек>. Текст этого задания не является техническим ограничителем.

## Требования

1. <Один конкретный вход и ожидаемый результат>.
2. <Отказ и неизменность данных>.
3. <Повтор / конфликт версий / права — по риску>.

Вне scope: <явные исключения>. Контракт API/XML/Oracle: <поля/типы/статусы/ошибки/версия>. Инвариант: <что не должно нарушиться>.

## Проверка

- Точные разрешённые команды: <существующие команды проекта; не придумывать зелёные результаты>.
- Новые тесты: <названия и assertions>.
- Данные: <обезличенный fixture>; Oracle: <только тестовый endpoint без пароля в задании>.
- UI: <состояния, scanner/keyboard, переходы, скриншоты>.
- Slow SQL: <как собрать; порог; кто принимает решение по longest queries>.

## Stop rules

Остановиться и сообщить, если нужен файл вне списка, новая сущность/миграция вне ADR, нет контракта, бюджет исчерпан, две неудачные попытки, тест требует недоступной системы или меняется физический смысл события. Не подменять реальное испытание mock-тестом.

## Результат

Diff + список изменённых файлов + выполненные команды/exit codes + непройденные проверки + usage/model/duration + известные риски. Не заявлять весь спринт завершённым по одному ticket. Принимает независимый reviewer в новой сессии.

